export const getFilms = (state) => {
    return state.movies.movies
};

export const getSearchWord = (state) => {
    return state.movies.searchWord
};

export const getListType = (state) => {
    return state.movies.listType
};